"""Frozen, version-controlled fixture data for this repo.

Fixtures here are plain data (JSON), not test code, so they can be shared by
multiple consumers: unit/integration tests that validate current behavior
against them, and (per PB-3b, Wave 2 of the repo-cleanup plan) a
recorded-fixture ``WebSearchProvider`` implementation that replays them as if
they came from a live provider.
"""
